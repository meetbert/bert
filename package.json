{
  "name": "bert-root",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "build:dev": "cd frontend && npm install && npm run build:dev",
    "test": "cd frontend && npm run test"
  }
}
